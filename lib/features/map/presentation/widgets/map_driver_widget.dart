import 'package:flutter/material.dart';

class MapDriverWidget extends StatelessWidget {
  const MapDriverWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Map Driver')),
      body: Center(child: const Text('Map Driver Widget')),
    );
  }
}
